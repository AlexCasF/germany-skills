# TypeScript / Node.js implementation

Build with:

```powershell
cd skills\destatis\typescript
npm install
npm run build
```

Run with:

```powershell
node skills\destatis\typescript\dist\index.js doctor
```

The TypeScript source compiles to Node.js JavaScript and mirrors the Go 2.0
command contract.
