# Python Implementation

Run from the repository root:

```text
python skills\bundestag-live\python\bundestagctl.py --help
```

The Python version has no third-party dependencies and mirrors the Go 2.0 command vocabulary for portability and comparison testing.
