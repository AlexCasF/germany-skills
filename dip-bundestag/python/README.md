# DIP Python CLI

`dipctl.py` is a Python stdlib implementation of the DIP CLI behavior.

Run from the repo root:

```powershell
python skills\dip-bundestag\python\dipctl.py --help
```

It uses `DIP_API_KEY` unless `--apikey` is passed.

