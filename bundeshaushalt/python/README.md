# Python Bundeshaushalt CLI

Run from the repository root:

```powershell
python skills\bundeshaushalt\python\bundeshaushaltctl.py doctor
python skills\bundeshaushalt\python\bundeshaushaltctl.py budget tree --year 2026 --account expenses --limit 5
```

The Python version mirrors the Go 2.0 command contract and uses only the Python standard library.
