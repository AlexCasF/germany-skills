# Python Tagesschau CLI

Run from the repository root:

```powershell
python skills\tagesschau\python\tagesschauctl.py doctor
python skills\tagesschau\python\tagesschauctl.py search --text Bundestag --limit 5
```

The Python version mirrors the Go 2.0 command contract and uses only the Python standard library.
