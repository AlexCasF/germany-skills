# dashboard-deutschland TypeScript / Node.js

TypeScript / Node.js implementation of the Dashboard Deutschland research CLI.

```powershell
npm install
npm run build
node dist/index.js doctor
node dist/index.js indicator data --id <indicator-id> --limit 3
```
